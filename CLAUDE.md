<!-- TRIGGER.DEV basic START -->

# Trigger.dev

For Trigger.dev (v4) task patterns — `task`/`schemaTask`, triggering, debounce, waits, and the deprecated APIs to avoid — use the `trigger-dev-tasks` skill.

<!-- TRIGGER.DEV basic END -->
