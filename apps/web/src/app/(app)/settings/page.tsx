import { SettingsView } from '@/features/settings/SettingsView';

/**
 * /settings — profile information and account management.
 */
export default function SettingsPage() {
  return <SettingsView />;
}
